/* Voyons les differents types de fonctions, les differentes manieres de les declarer */
function a(x: number) {
  return x * 2;
}

const b = (x: number) => {
  return x * 2;
}

const c = (x: number) => x * 2
